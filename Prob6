#include <iostream>
#include <conio.h>


using namespace std;
int main()
{
	int x, i, z, y;
 	y = 1;
	
	cout<<"0,1,";
	
	for ( i = 0 ; i < 20 ; i++ )
	{
		if (i<19)
		{
		z = x + y;
		x = y;
		y = z;
		
		cout << y << ",";
		}
		else if (i==19)
		cout << x + y;
	}
	_getch();
	return 0;
}
